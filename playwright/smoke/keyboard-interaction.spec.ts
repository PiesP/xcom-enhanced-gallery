import { test } from '@playwright/test';

test.describe.skip('Keyboard performance metrics deprecated', () => {
  test('deprecated harness metrics removed', () => {
    // Placeholder suite retained so historical test IDs remain referenced until docs fully migrate.
    // No runtime behavior: harness no longer exposes performance measurement APIs.
  });
});
