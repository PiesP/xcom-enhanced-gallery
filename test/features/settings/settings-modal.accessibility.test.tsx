// Legacy TSX duplicate intentionally left as a placeholder.
// Active SettingsModal accessibility tests live in:
//  - test/features/settings/settings-modal.accessibility.smoke.test.ts (panel)
//  - test/features/settings/settings-modal.modal-accessibility.smoke.test.ts (modal)
// This file remains to document historical location; define a skipped placeholder to avoid per-file failure.

import { describe, it } from 'vitest';

describe.skip('[legacy] SettingsModal accessibility placeholder', () => {
  it('placeholder — see smoke tests for coverage', () => {
    // intentionally empty
  });
});
