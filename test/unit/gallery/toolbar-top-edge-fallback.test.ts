// Legacy duplicate test file.
// Real executable tests live in the corresponding .tsx variant.
// Provide a skipped suite so Vitest doesn't treat file as error ("No test suite found").
describe.skip('legacy: toolbar-top-edge-fallback duplicate placeholder', () => {
  it.skip('placeholder', () => {
    /* no-op */
  });
});

export {};
