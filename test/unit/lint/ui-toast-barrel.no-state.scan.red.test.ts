import { describe, it } from 'vitest';

// Deprecated placeholder — replaced by guard test:
// test/unit/lint/toast-ui-barrel.stateful-exports.guard.test.ts
// Keeping as skipped to avoid "No test suite found" failures while preserving history.
describe('[deprecated] ui-toast-barrel.no-state.scan.red', () => {
  it.skip('Replaced by toast-ui-barrel.stateful-exports.guard.test.ts', () => {
    // intentionally empty
  });
});
