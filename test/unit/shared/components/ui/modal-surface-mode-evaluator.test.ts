import { describe, it, expect } from 'vitest';
// WHY SKIPPED: 단위 evaluator 테스트가 통합 suite 로 흡수되어 중복.
describe.skip('modal-surface-mode-evaluator (archived placeholder)', () => {
  it('placeholder', () => expect(true).toBe(true));
});
