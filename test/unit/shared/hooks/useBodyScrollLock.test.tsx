import { describe, it, expect } from 'vitest';

// Duplicate placeholder: original test moved to .test.ts; this file kept intentionally to satisfy any references.
describe('useBodyScrollLock duplicate placeholder', () => {
  it.skip('placeholder (duplicate neutralized)', () => {
    expect(true).toBe(true);
  });
});
