import { describe, it } from 'vitest';

// Deprecated placeholder — replaced by guard test:
// test/unit/lint/toast-ui-components.no-local-state.guard.test.ts
// Keeping as skipped to avoid "No test suite found" failures while preserving history.
describe('[deprecated] ui-toast-component.no-local-state.scan.red', () => {
  it.skip('Replaced by toast-ui-components.no-local-state.guard.test.ts', () => {
    // intentionally empty
  });
});
