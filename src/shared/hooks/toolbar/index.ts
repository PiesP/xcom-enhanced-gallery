/**
 * @deprecated Import from '@shared/hooks' instead.
 * Keeping this file as a thin re-export avoids breaking legacy test helpers
 * while steering application code toward the shared hooks barrel.
 */
export {
  useToolbarSettingsController,
  type UseToolbarSettingsControllerOptions,
  type ToolbarSettingsControllerResult,
} from './use-toolbar-settings-controller';
