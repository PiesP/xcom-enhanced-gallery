/**
 * Transitional shim (will be removed after all imports migrate to iconRegistry exports)
 * Prevents orphan noise by keeping path while delegating to unified source.
 */
export { CORE_ICONS, isCoreIcon } from './iconRegistry';
