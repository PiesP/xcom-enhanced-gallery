/**
 * Interfaces Barrel Export
 *
 * @version 2.0.0 - Phase 352: Named export 최적화
 * Phase 200: 타입 계층 통합
 * - GalleryRenderer: Features 계층 렌더러 계약 정의
 * - GalleryRenderOptions: 렌더 옵션 (types/media.types.ts가 기준)
 *
 * 참고: 대부분의 서비스 인터페이스는 @shared/types/media.types.ts에 정의됩니다.
 */

export type { GalleryRenderer, GalleryRenderOptions } from './gallery.interfaces';
