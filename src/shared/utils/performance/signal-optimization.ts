/**
 * @fileoverview Signal Selector Optimization (Legacy Shim)
 * @description Re-exports the canonical implementations from `@shared/utils/signal-selector`.
 *              This keeps backward compatibility while preventing duplicate sources.
 */

export * from '../signal-selector';
