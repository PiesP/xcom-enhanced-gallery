/**
 * Deprecated: specialized scheduler helpers have been removed.
 * This type-only placeholder prevents dependency-cruiser from flagging the
 * module as an orphan while ensuring nothing ships in the runtime bundle.
 */
export type DeprecatedSchedulers = never;
