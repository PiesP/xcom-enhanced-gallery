/**
 * Accessibility root facade
 * 중복 구현을 제거하고 내부 배럴(`./accessibility/index`)을 그대로 노출합니다.
 * import 경로 `@/shared/utils/accessibility`에서도 동일 표면을 제공하기 위함입니다.
 */
export * from './accessibility/index';
