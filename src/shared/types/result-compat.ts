/**
 * Legacy result compatibility helpers were fully removed.
 *
 * This placeholder file remains to avoid breakages for stale imports,
 * but it intentionally exports nothing. Consumers should rely on
 * `@shared/types/result.types` utilities directly.
 */
export {};
