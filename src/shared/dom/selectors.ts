/**
 * Standard selector registry entrypoint.
 *
 * Prefer importing selectors and selector helpers from this module in new code.
 */
export {
  FALLBACK_SELECTORS,
  queryAllWithFallback,
  queryWithFallback,
  SELECTORS,
  STABLE_SELECTORS,
} from '@constants/selectors';
