/**
 * Entry point for gallery icons.
 *
 * Exports the minimal data-driven HeroIcon component plus the base Icon wrapper.
 */

export { HeroIcon, type HeroIconProps } from './hero/hero-icons';
export { Icon, type IconProps } from './Icon';
