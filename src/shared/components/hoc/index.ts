/* dependency-cruiser: ignore */

/**
 * @deprecated The gallery HOC was removed to reduce bundle size.
 * This placeholder module intentionally exports nothing but remains in place
 * to avoid breaking legacy tooling that might still resolve this path.
 */
export {};
