/** Settings module types — re-export from shared for backward compatibility */

export type {
  AppSettings,
  FeatureFlags,
  NestedSettingKey,
  SettingChangeEvent,
} from '@shared/types/settings.types';
