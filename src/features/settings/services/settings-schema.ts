/**
 * @fileoverview Settings schema hash constant.
 *
 * Bump this value whenever the persisted settings shape changes in a way that
 * should trigger a migration re-save.
 */
export const SETTINGS_SCHEMA_HASH = '1';
