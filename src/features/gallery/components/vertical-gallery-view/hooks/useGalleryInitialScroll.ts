/* dependency-cruiser: ignore */

/**
 * @deprecated The vertical gallery no longer relies on a dedicated initial scroll hook.
 * Scroll alignment is handled inside useGalleryItemScroll directly.
 */
export {};
