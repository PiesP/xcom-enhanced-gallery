{
  "name": "xcom-enhanced-gallery",
  "version": "2.3.1",
  "description": "Desktop media gallery and original-quality downloader for X.com.",
  "type": "module",
  "private": true,
  "scripts": {
    "preinstall": "test -f packages/core/package.json || { echo '⚠️  Submodule not initialized. Run: git submodule sync --recursive && git submodule update --init --recursive'; exit 1; }",
    "check:prebuild": "pnpm -s check:versions && pnpm -s quality",
    "prebuild": "pnpm -s check:prebuild",
    "prebuild:extension": "pnpm -s check:prebuild",
    "prebuild:extension:firefox": "pnpm -s check:prebuild",
    "prebuild:all": "pnpm -s check:prebuild",
    "build": "vite build",
    "build:extension": "vite build --config vite.extension.config.ts && vite build --config vite.extension.cs.config.ts && node --experimental-strip-types scripts/check/extension-build.ts dist-extension",
    "build:extension:firefox": "vite build --config vite.extension.firefox.config.ts && vite build --config vite.extension.firefox.cs.config.ts && node --experimental-strip-types scripts/check/extension-build.ts dist-extension-firefox",
    "build:all": "pnpm -s build:ci && pnpm -s build:extension:ci && pnpm -s build:extension:firefox:ci",
    "build:dev": "vite build --mode development",
    "build:fast": "vite build",
    "package:extension": "node --experimental-strip-types scripts/release/package-extension.ts",
    "package:all": "pnpm build:all && pnpm package:extension",
    "release:prepare": "node --experimental-strip-types scripts/release/prepare.ts",
    "security:codex:dry-run": "scripts/security/codex-security.sh dry-run",
    "security:codex:working-tree": "scripts/security/codex-security.sh working-tree",
    "security:codex:branch": "scripts/security/codex-security.sh branch",
    "security:codex:full": "scripts/security/codex-security.sh full",
    "build:ci": "vite build",
    "build:extension:ci": "vite build --config vite.extension.config.ts && vite build --config vite.extension.cs.config.ts && node --experimental-strip-types scripts/check/extension-build.ts dist-extension",
    "build:extension:firefox:ci": "vite build --config vite.extension.firefox.config.ts && vite build --config vite.extension.firefox.cs.config.ts && node --experimental-strip-types scripts/check/extension-build.ts dist-extension-firefox",
    "build:all:ci": "pnpm -s check:versions && pnpm -s build:ci && pnpm -s build:extension:ci && pnpm -s build:extension:firefox:ci",
    "build:e2e": "pnpm clean && pnpm build:ci && pnpm build:dev && pnpm build:extension:ci && pnpm build:extension:firefox:ci",
    "clean": "node --experimental-strip-types scripts/build/clean.ts",
    "mut": "stryker run --reporters=progress",
    "mut:fast": "stryker run stryker.conf.fast.json",
    "check:src": "tsc --noEmit",
    "check": "pnpm check:src",
    "check:versions": "node --experimental-strip-types scripts/release/version.ts check",
    "sync:versions": "node --experimental-strip-types scripts/release/version.ts sync",
    "typecheck": "pnpm -s check",
    "knip": "knip --dependencies",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "fmt": "biome check --linter-enabled=false .",
    "fmt:fix": "biome check --write --linter-enabled=false .",
    "fmt:check": "pnpm -s fmt",
    "circular": "dpdm --tsconfig tsconfig.json --circular --no-tree --no-warning --exit-code circular:1 src/**/*.ts src/**/*.tsx",
    "quality": "pnpm -s fmt && pnpm -s lint && pnpm -s check && pnpm -s check:e2e && pnpm -s circular && pnpm -s knip && pnpm -s quality:nose",
    "quality:fix": "pnpm -s fmt:fix && pnpm -s lint:fix && pnpm -s quality",
    "quality:nose": "bash -c 'if command -v nose &>/dev/null; then nose query src --baseline .nose-baseline.json --fail-on new; else echo \"[nose] not installed — skipping\"; fi'",
    "verify": "pnpm -s quality && pnpm -s build:all:ci",
    "verify:full": "pnpm verify && pnpm test:cov && pnpm build:dev && pnpm -s test:e2e:all",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:e2e": "playwright test --config test/e2e/playwright.config.ts",
    "test:e2e:extension": "playwright test --config test/e2e/playwright.extension.config.ts",
    "test:e2e:extension:firefox": "node --experimental-strip-types --test test/e2e/firefox-extension-runtime.test.ts",
    "test:e2e:all": "pnpm -s test:e2e && pnpm -s test:e2e:extension && pnpm -s test:e2e:extension:firefox",
    "check:e2e": "tsc -p tsconfig.e2e.json --noEmit",
    "test:e2e:headed": "playwright test --config test/e2e/playwright.config.ts --headed"
  },
  "keywords": [
    "userscript",
    "x.com",
    "twitter",
    "gallery",
    "media",
    "browser-extension"
  ],
  "author": "PiesP",
  "license": "MIT",
  "engines": {
    "node": ">=22.13.0",
    "pnpm": ">=11.17.0"
  },
  "packageManager": "pnpm@11.17.0",
  "volta": {
    "node": "26.8.1",
    "pnpm": "11.17.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/PiesP/xcom-enhanced-gallery"
  },
  "bugs": {
    "url": "https://github.com/PiesP/xcom-enhanced-gallery/issues"
  },
  "homepage": "https://github.com/PiesP/xcom-enhanced-gallery#readme",
  "dependencies": {
    "@piesp/browser-core": "file:packages/core",
    "solid-js": "^1.9.15"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@biomejs/biome": "^2.5.11",
    "@playwright/test": "^1.62.1",
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^10.0.0",
    "@types/node": "^26.4.0",
    "@types/selenium-webdriver": "4.35.6",
    "@vitest/coverage-v8": "^4.1.11",
    "dpdm": "4.3.0",
    "jsdom": "^30.0.1",
    "knip": "^6.34.0",
    "selenium-webdriver": "4.48.0",
    "typescript": "7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-solid": "^2.11.14",
    "vitest": "^4.1.11"
  }
}
