name: 🏗️ CI Pipeline

# Responsibility: Continuous Integration
# - Linting (ESLint with SARIF reporting)
# - Type checking (TypeScript)
# - Build verification (Production mode)
#
# Triggered on: Push/PR to master (excluding docs/assets)

on:
  push:
    branches: [master]
    paths-ignore:
      - "docs/**"
      - "**/*.md"
      - "**/*.MD"
      - "**/*.txt"
      - "**/*.svg"
      - ".vscode/**"
      - ".github/*.md"
      - "LICENSES/**"
  pull_request:
    branches: [master]
    paths-ignore:
      - "docs/**"
      - "**/*.md"
      - "**/*.MD"
      - "**/*.txt"
      - "**/*.svg"
      - ".vscode/**"
      - ".github/*.md"
      - "LICENSES/**"
  workflow_dispatch:

env:
  NODE_VERSION: "24"
  CI: "true"
  XEG_DISABLE_LOCAL_CONFIG: "true"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Run lint and typecheck in parallel for faster feedback
  lint:
    name: 🧹 Biome Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            src
            package.json
            package-lock.json
            tsconfig.json
            biome.json
            .github/actions
          sparse-checkout-cone-mode: false

      - name: 🔧 Setup environment
        uses: ./.github/actions/setup-node
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 🔍 Run Biome
        run: npx biome check src --max-diagnostics=100

  typecheck:
    name: 📐 Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            src
            package.json
            package-lock.json
            tsconfig.json
            .github/actions
          sparse-checkout-cone-mode: false

      - name: 🔧 Setup environment
        uses: ./.github/actions/setup-node
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📐 Run TypeScript check
        run: npx tsc --noEmit --incremental --tsBuildInfoFile .tsbuildinfo

  build:
    name: 🚀 Build
    runs-on: ubuntu-latest
    needs: [lint, typecheck]
    timeout-minutes: 15
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6

      - name: 🔧 Setup environment
        uses: ./.github/actions/setup-node
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Cache Vite build
        uses: actions/cache@v4
        with:
          path: |
            node_modules/.vite
            .vite
          key: vite-${{ runner.os }}-${{ hashFiles('package-lock.json', 'vite.config.ts') }}
          restore-keys: |
            vite-${{ runner.os }}-

      - name: 🚀 Build production
        run: npx vite build --mode production

      - name: 📊 Check bundle size
        run: |
          SIZE=$(stat -f%z dist/xcom-enhanced-gallery.user.js 2>/dev/null || stat -c%s dist/xcom-enhanced-gallery.user.js)
          echo "📦 Bundle size: $(numfmt --to=iec-i --suffix=B $SIZE)"
          # Fail if bundle exceeds 350KB
          if [ $SIZE -gt 358400 ]; then
            echo "❌ Bundle size exceeds 350KB limit!"
            exit 1
          fi

      - name: 📊 Upload build artifacts
        uses: actions/upload-artifact@v5
        if: success()
        with:
          name: build-${{ github.sha }}
          path: dist/
          retention-days: 7
          compression-level: 9
