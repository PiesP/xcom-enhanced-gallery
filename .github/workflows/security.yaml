name: 🔒 Security Scanning

# Responsibility: Security analysis for Deno-based project
# - OSV scanning: vulnerability detection in dependencies
# - SAST: static security analysis with Semgrep
# - License compliance: verify approved licenses (MIT/ISC/BSD/Apache-2.0)
#
# Trigger: Weekly schedule + push to master + PR to master

on:
  schedule:
    - cron: "0 1 * * 0" # Weekly on Sundays at 1 AM UTC
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

env:
  DENO_VERSION: "2.x"

permissions:
  contents: read
  security-events: write

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  osv-scanner:
    name: 🛡️ OSV Vulnerability Scan
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Install OSV-Scanner
        run: |
          curl -fsSL https://github.com/google/osv-scanner/releases/latest/download/osv-scanner_linux_amd64 -o osv-scanner
          chmod +x osv-scanner
          sudo mv osv-scanner /usr/local/bin/

      - name: 🔍 Run OSV-Scanner
        run: |
          osv-scanner --lockfile=deno.lock --format=sarif --output=osv-results.sarif . || true
        continue-on-error: true

      - name: 📤 Upload OSV results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: osv-results.sarif
        continue-on-error: true

  semgrep:
    name: 🔍 Static Analysis (Semgrep)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Run Semgrep
        uses: semgrep/semgrep-action@v1
        with:
          config: >-
            p/typescript
            p/javascript
            p/security-audit
            p/secrets
        env:
          SEMGREP_RULES: >-
            p/typescript
            p/javascript
            p/security-audit
            p/secrets

  license-compliance:
    name: 📜 License Compliance Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🦕 Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: ${{ env.DENO_VERSION }}

      - name: 📜 Verify LICENSES directory
        run: |
          set -euo pipefail
          echo "📜 Checking license files..."

          LICENSES_DIR="LICENSES"
          if [ ! -d "$LICENSES_DIR" ]; then
            echo "⚠️ LICENSES directory not found"
            exit 1
          fi

          # Check that required license files exist
          REQUIRED_LICENSES=(
            "xcom-enhanced-gallery-MIT.txt"
            "solid-js-MIT.txt"
            "heroicons-MIT.txt"
          )

          MISSING_LICENSES=()
          for license in "${REQUIRED_LICENSES[@]}"; do
            if [ ! -f "$LICENSES_DIR/$license" ]; then
              MISSING_LICENSES+=("$license")
            fi
          done

          if [ ${#MISSING_LICENSES[@]} -ne 0 ]; then
            echo "❌ Missing license files:"
            printf '  - %s\n' "${MISSING_LICENSES[@]}"
            exit 1
          fi

          echo "✅ All required license files present"
          ls -la "$LICENSES_DIR"

      - name: 🔍 Check for disallowed licenses
        run: |
          set -euo pipefail
          echo "🔍 Checking for disallowed license content..."

          # List of disallowed license types (GPL variants, AGPL, proprietary)
          DISALLOWED_PATTERNS=(
            "GNU General Public License"
            "GNU GPL"
            "GNU AGPL"
            "AGPL-3.0"
            "GPL-2.0"
            "GPL-3.0"
            "proprietary"
            "all rights reserved"
          )

          FOUND_ISSUES=0
          for pattern in "${DISALLOWED_PATTERNS[@]}"; do
            if grep -ri "$pattern" LICENSES/ 2>/dev/null; then
              echo "⚠️ Found potentially disallowed license: $pattern"
              FOUND_ISSUES=1
            fi
          done

          if [ $FOUND_ISSUES -eq 1 ]; then
            echo "❌ Potential license compliance issues found"
            # Don't fail, just warn - manual review needed
          else
            echo "✅ No disallowed license patterns found"
          fi

  security-summary:
    name: 📊 Security Summary
    runs-on: ubuntu-latest
    needs: [osv-scanner, semgrep, license-compliance]
    if: always()
    timeout-minutes: 5
    steps:
      - name: 📊 Generate summary
        run: |
          echo "## 🔒 Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Status | Check | Result |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|--------|" >> $GITHUB_STEP_SUMMARY

          # OSV Scanner
          if [ "${{ needs.osv-scanner.result }}" == "success" ]; then
            echo "| ✅ | OSV Vulnerability Scan | Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ⚠️ | OSV Vulnerability Scan | Check required |" >> $GITHUB_STEP_SUMMARY
          fi

          # Semgrep
          if [ "${{ needs.semgrep.result }}" == "success" ]; then
            echo "| ✅ | Static Analysis (Semgrep) | Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ⚠️ | Static Analysis (Semgrep) | Check required |" >> $GITHUB_STEP_SUMMARY
          fi

          # License
          if [ "${{ needs.license-compliance.result }}" == "success" ]; then
            echo "| ✅ | License Compliance | Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ | License Compliance | Failed |" >> $GITHUB_STEP_SUMMARY
          fi
