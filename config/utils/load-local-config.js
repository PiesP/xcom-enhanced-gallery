/*
 * Local configuration loader - stub for CI (production) builds
 * This file is intentionally minimal and returns null to avoid
 * requiring a local dev configuration when running on CI.
 */
export async function loadLocalConfig() {
  return null;
}
