/**
 * TS bridge to JS implementation
 *
 * Context: Docs mention validate-build.ts (Phase 296),
 * but the implementation currently lives in validate-build.js.
 * This thin wrapper keeps package.json and docs consistent while reusing the JS.
 */
import './validate-build.js';
