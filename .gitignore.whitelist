# ============================================================================
# 화이트리스트 방식 .gitignore (포함할 것만 명시)
# ============================================================================
# 기본 규칙:
# 1. 기본적으로 모든 것을 무시 (*)
# 2. 필요한 디렉토리와 파일만 추적 (!path)
# 3. 추적된 디렉토리 내 모든 파일도 추적
# ============================================================================

# 모든 것을 무시 (기본값)
*

# ============================================================================
# TRACKED DIRECTORIES & FILES (원격 저장소에 포함)
# ============================================================================

# === Source Code (필수) ===
!src/
!src/**

# === TypeScript Definitions (필수) ===
!types/
!types/**

# === Test Code (필수) ===
!test/
!test/**
!playwright/
!playwright/**

# === Configuration Files (필수 빌드 설정) ===
!tsconfig.base.json
!tsconfig.json
!tsconfig.tests.json
!vite.config.ts
!vitest.config.ts
!eslint.config.js
!postcss.config.js
!playwright.config.ts

# === Package Management (필수) ===
!package.json
!package-lock.json
!.npmrc

# === Documentation (필수) ===
!README.md
!LICENSE
!CHANGELOG.md
!AGENTS.md
!CLAUDE.md

# === Documentation Directory (필수 가이드) ===
!docs/
!docs/**

# === Build Output - UserScript Only (배포용) ===
# dist/*.user.js 는 배포용으로 추적
!dist/
!dist/*.user.js

# === Release Artifacts (배포 메타데이터) ===
!release/
!release/RELEASE_NOTES.md

# === GitHub Configuration (CI/CD 설정) ===
!.github/
!.github/**

# === License Files (라이선스) ===
!LICENSES/
!LICENSES/**

# === Configuration & Overrides (config 헬퍼만) ===
!config/
!config/utils/
!config/utils/load-local-config.js
!config/utils/load-local-config.d.ts
!config/local/
!config/local/README.md

# === Scripts (공용 스크립트만) ===
!scripts/
!scripts/validate-build.ts
!scripts/analyze-solid-apis.ts
!scripts/apply-test-isolation.ts
!scripts/cleanup-vitest-workers.ts
!scripts/generate-dep-graph.ts
!scripts/maintenance-check.ts
!scripts/protect-vscode.ts
!scripts/run-all-tests.ts
!scripts/run-fast-batched.ts
!scripts/run-unit-tests-batched.ts
!scripts/run-vitest-project.ts
!scripts/test-perplexity-api.ts

# === Git Configuration (필수) ===
!.git
!.gitignore
!.gitignore.whitelist
!.gitattributes
!.gitkeep

# ============================================================================
# EXCLUDED (원격 저장소에 포함되지 않음)
# ============================================================================

# === Node.js & Dependencies ===
node_modules/
.pnp/
.yarn/
.pnpm-store/

# === Build Artifacts ===
dist-ssr/
build/
.vite/
.cache/

# === Test & Coverage ===
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.vitest-cache/
**/__screenshots__/

# === CodeQL & Security ===
.codeql-db/
codeql-db/
codeql-results/
codeql-reports/

# === Development Logs ===
logs/
*.log
*.cpuprofile
vitest-*.log

# === Local Configuration ===
.env
.env.local
.env.*.local
.env*.example

# === IDE & Editor ===
.husky/
.vscode/
.idea/
*.swp
*.swo
.vs/
*.sublime-*
.atom/
.fleet/
*.code-workspace

# === OS Files ===
.DS_Store
.AppleDouble
._*
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# === Tool Caches ===
.dependency-cruiser-cache
.stylelintcache
.eslintcache
.prettiercache
.markdownlintcache
.tscache/

# === TypeScript ===
*.tsbuildinfo

# === Temporary Files ===
temp/
tmp/
backup*/
*.backup
*.backup.*
*.bak
*.old
*.orig
*~
*.tmp
*.temp

# === Developer Overrides ===
*.local.js
*.local.mjs
*.local.ts
*.local.json
config/local/*
!config/local/README.md

# === Development Folders ===
scratch/
playground/
experiments/
sandbox/
metrics/

# === Generated Files ===
*.js.map
*.css.map
*.user.js.map

# === Security (Local) ===
*.pem
*.p12
secret.key
ssl/
certs/

# === Root Temporaries ===
build-*.txt
test-*.txt
validate-*.txt
release/checksums.txt
release/metadata.json
release/*.user.js
