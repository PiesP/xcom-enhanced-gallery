# Whitelist-based .gitignore for xcom-enhanced-gallery
# 원격 저장소: 소스 코드 + 빌드된 스크립트 + CI/CD 설정만 추적
# 로컬만 처리: 모든 캐시, 테스트 결과, 빌드 중간산출물, 개발 설정

# ============================================================================
# 기본 규칙: 모든 것을 무시하고 필요한 것만 추적
# ============================================================================

# 전체 무시
*
!*/

# ============================================================================
# TRACKED: 원격 저장소에 포함되는 파일들
# ============================================================================

# --- 소스 코드 (필수) ---
!/src
!/src/**

# --- 타입 정의 (필수) ---
!/types
!/types/**

# --- 테스트 코드 (필수) ---
!/test
!/test/**
!/playwright
!/playwright/**

# --- 필수 빌드 설정 ---
!/tsconfig.base.json
!/tsconfig.json
!/tsconfig.tests.json
!/vite.config.ts
!/vitest.config.ts
!/eslint.config.js
!/postcss.config.js
!/playwright.config.ts

# --- 패키지 관리 ---
!/package.json
!/package-lock.json
!/.npmrc

# --- 핵심 문서 ---
!/README.md
!/LICENSE
!/CHANGELOG.md
!/AGENTS.md
!/CLAUDE.md

# --- 문서 디렉토리 ---
!/docs
!/docs/**

# --- 빌드된 배포 스크립트 (UserScript만) ---
!/dist
!/dist/*.user.js

# --- 릴리스 정보 ---
!/release
!/release/RELEASE_NOTES.md

# --- GitHub Actions & CI/CD ---
!/.github
!/.github/**

# --- 라이선스 파일 ---
!/LICENSES
!/LICENSES/**

# --- 설정 헬퍼 (로컬 오버라이드 시스템) ---
!/config
!/config/utils
!/config/utils/load-local-config.js
!/config/utils/load-local-config.d.ts
!/config/local
!/config/local/README.md

# --- 공용 스크립트 ---
!/scripts
!/scripts/validate-build.js
!/scripts/validate-build.ts
!/scripts/check-codeql.js
!/scripts/cleanup-vitest-workers.js
!/scripts/generate-dep-graph.js
!/scripts/maintenance-check.js
!/scripts/protect-vscode.js
!/scripts/run-all-tests.js
!/scripts/run-fast-batched.js
!/scripts/run-vitest-project.js

# --- Git 설정 ---
!/.git
!/.gitignore
!/.gitignore.whitelist
!/.gitattributes

# ============================================================================
# IGNORED: 원격 저장소에 포함되지 않는 것들 (모두 로컬 처리)
# ============================================================================

# node_modules와 의존성 캐시
node_modules/
.pnp/
.yarn/
.pnpm-store/

# 빌드 관련 (배포용 UserScript 제외)
dist-ssr/
build/
.vite/
.cache/

# 테스트와 커버리지
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.vitest-cache/
**/__screenshots__/

# CodeQL 및 보안 (로컬 스캔 결과만)
.codeql-db/
codeql-db/
codeql-results/
codeql-reports/

# 로그 파일
logs/
*.log
*.cpuprofile
vitest-*.log

# 환경 변수 (로컬만)
.env
.env.local
.env.*.local

# IDE & 에디터
.husky/
.vscode/
.idea/
*.swp
*.swo
.vs/
*.sublime-*
.atom/
.fleet/
*.code-workspace

# 운영체제 파일
.DS_Store
.AppleDouble
._*
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# 도구 캐시 (모두 로컬)
.dependency-cruiser-cache
.stylelintcache
.eslintcache
.prettiercache
.markdownlintcache
.tscache/

# TypeScript 중간 파일
*.tsbuildinfo

# 임시 파일
temp/
tmp/
backup*/
*.backup
*.backup.*
*.bak
*.old
*.orig
*~
*.tmp
*.temp

# 개발자 로컬 설정 (오버라이드)
*.local.js
*.local.mjs
*.local.ts
*.local.json

# 개발 전용 폴더
scratch/
playground/
experiments/
sandbox/
metrics/

# 생성된 맵 파일
*.js.map
*.css.map
*.user.js.map

# 보안 파일 (로컬만)
*.pem
*.p12
secret.key
ssl/
certs/

# 루트 임시 파일
build-*.txt
test-*.txt
validate-*.txt
release/checksums.txt
release/metadata.json
release/*.user.js

# 완료된 문서 아카이브
docs/archive/**
docs/temp/**

```
