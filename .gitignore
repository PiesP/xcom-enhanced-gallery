# Whitelist-based .gitignore for xcom-enhanced-gallery (Minimal Repository)
# Remote repository: track only source code, built scripts, and CI/CD configs
# Local only: ignore caches, test outputs, build artifacts, and developer settings

# ============================================================================
# Guideline: ignore everything and re-allow only required assets
# ============================================================================

# Ignore all
*
!*/

# ============================================================================
# TRACKED: Files included in the remote repo (required items only)
# ============================================================================

# --- Source code (required) ---
!/src
!/src/**

# --- Type definitions (required) ---
!/types
!/types/**

# --- Critical build config (deployment required) ---
!/tsconfig.base.json
!/tsconfig.json
!/vite.config.ts

# --- Package management ---
!/package.json
!/package-lock.json
!/.npmrc

# --- Quality tooling (CI required) ---
!/.prettierrc.json
!/eslint.config.js

# --- Core documentation ---
!/README.md
!/LICENSE
!/CHANGELOG.md
!/CONTRIBUTING.md
!/CODE_OF_CONDUCT.md

# --- GitHub Actions (deploy/security) ---
!/.github
!/.github/**

# --- Dependency licenses (legal requirement, referenced by vite.config.ts) ---
!/LICENSES
!/LICENSES/**

# --- Git configuration ---
!/.gitignore

# ============================================================================
# NOT TRACKED: Local-only assets (developer tooling)
# ============================================================================

# Development guide docs (local reference)
.github/copilot-instructions.md
/AGENTS.md
/CLAUDE.md
/docs/

# Test code (not run in CI)
/test/**
/playwright/

# Developer scripts and tools
/scripts/**
# NOTE: The `config/` directory contains developer-specific configuration and is
# ignored by default. The file `config/utils/load-local-config.js` however is a
# CI-safe stub required for builds in the action runners. We explicitly un-ignore
# it so the file can be committed while keeping the rest of the `config/` dir
# as local-only entries.
/config/
!/config/utils/load-local-config.js
!/config/utils/load-local-config.d.ts

# Local configuration
/*.local.ts
/*.local.js
/*.local.json
/.env.local
/.env.*.local
/config/local/*

# TypeScript test config (CI only)
/tsconfig.tests.json

# Build config (optional)
/postcss.config.js
/playwright.config.ts
/vitest.config.ts

# ============================================================================
# BUILD & CACHE (local only)
# ============================================================================

# Vite & Build
/dist/
/dist-ssr/
/build/
/.vite/
.tsbuildinfo
/src/shared/types/generated/design-token-names.ts

# Test cache and results
/coverage/
/test-results/
/playwright-report/
/.vitest-cache/
.eslintcache
.prettiercache
.stylelintcache

# Dependency caches
/node_modules/
/pnpm-lock.yaml
/yarn.lock

# Development tools
/.husky/
/.idea/
/.vscode/
/codeql-results/
/codeql-reports/

# OS
/.DS_Store
/Thumbs.db

# Temporary and backup files
/temp/
/tmp/
/backup*/
/*.backup
/.stryker-tmp/
*.log
*.cpuprofile
