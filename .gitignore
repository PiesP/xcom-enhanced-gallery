# Whitelist-based .gitignore for xcom-enhanced-gallery (Minimal Repository)
# 원격 저장소: 소스 코드 + 빌드된 스크립트 + CI/CD 설정만 추적
# 로컬만 처리: 모든 캐시, 테스트 결과, 빌드 중간산출물, 개발 설정

# ============================================================================
# 기본 규칙: 모든 것을 무시하고 필수 파일만 추적
# ============================================================================

# 전체 무시
*
!*/

# ============================================================================
# TRACKED: 원격 저장소에 포함되는 파일들 (필수 항목만)
# ============================================================================

# --- 소스 코드 (필수) ---
!/src
!/src/**

# --- 타입 정의 (필수) ---
!/types
!/types/**

# --- 필수 빌드 설정 (배포 필수) ---
!/tsconfig.base.json
!/tsconfig.json
!/vite.config.ts

# --- 품질 검사 설정 (CI 필수) ---
!/eslint.config.cjs
!/stylelint.config.cjs

# --- 패키지 관리 ---
!/package.json
!/package-lock.json
!/.npmrc

# --- 품질 검사 설정 (CI 필수) ---
!/eslint.config.cjs
!/stylelint.config.cjs

# --- 핵심 문서 ---
!/README.md
!/LICENSE
!/CHANGELOG.md

# --- GitHub Actions (배포/보안) ---
!/.github
!/.github/**

# --- 의존성 라이선스 (법적 필요, vite.config.ts에서 참조) ---
!/LICENSES
!/LICENSES/**

# --- Git 설정 ---
!/.gitignore

# --- 로컬 전용 디렉토리 가이드 (문서만 추적) ---
!/playwright/README.md
!/playwright/.cache/README.md
!/scripts/README.md
!/scripts/temp/README.md
!/config/README.md

# ============================================================================
# NOT TRACKED: 로컬만 처리하는 파일들 (개발 도구)
# ============================================================================

# 개발 가이드 문서 (로컬 참고)
/AGENTS.md
/CLAUDE.md
/docs/

# 테스트 코드 (CI에서 실행)
/test/
/playwright/

# 개발 스크립트 및 도구
/scripts/**
!/scripts/run-unit-tests-batched.ts
!/scripts/validate-relative-imports.ts
# NOTE: The `config/` directory contains developer-specific configuration and is
# ignored by default. The file `config/utils/load-local-config.js` however is a
# CI-safe stub required for builds in the action runners. We explicitly un-ignore
# it so the file can be committed while keeping the rest of the `config/` dir
# as local-only entries.
/config/
!/config/utils/load-local-config.js
!/config/utils/load-local-config.d.ts

# 로컬 설정
/*.local.ts
/*.local.js
/*.local.json
/.env.local
/.env.*.local
/config/local/*

# TypeScript 테스트 설정 (CI만 사용)
/tsconfig.tests.json

# 빌드 설정 (옵션)
/postcss.config.js
/playwright.config.ts
/vitest.config.ts

# ============================================================================
# BUILD & CACHE (로컬만)
# ============================================================================

# Vite & Build
/dist/
/dist-ssr/
/build/
/.vite/
.tsbuildinfo

# 테스트 캐시 및 결과
/coverage/
/test-results/
/playwright-report/
/.vitest-cache/
.eslintcache
.prettiercache
.stylelintcache

# 의존성 캐시
/node_modules/
/pnpm-lock.yaml
/yarn.lock

# 개발 도구
/.husky/
/.idea/
/.vscode/
/codeql-results/
/codeql-reports/

# OS
/.DS_Store
/Thumbs.db

# 임시 및 백업
/temp/
/tmp/
/backup*/
/*.backup
*.log
*.cpuprofile
