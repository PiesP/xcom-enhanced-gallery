#!/usr/bin/env sh
# Pre-commit Hook: Lint and Format Staged Files
# ==============================================
# Purpose: Ensures code quality before commits
#
# Features:
#   - Lints TypeScript/JavaScript files (ESLint)
#   - Formats all files (Prettier)
#   - Fixes CSS issues (stylelint)
#   - Only processes staged files (efficient)
#
# Behavior:
#   - Skipped in CI environments (GitHub Actions handles full validation)
#   - Failures block the commit
#   - Uses .lintstagedrc.json configuration
#
# Configuration: See package.json "lint-staged" section
# For details: See AGENTS.md > Git Hooks

[ "$CI" = "true" ] && exit 0

echo "🧹 Running lint-staged on staged files..."
npm run lint:staged
