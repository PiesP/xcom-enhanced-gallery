#!/usr/bin/env sh

# Lightweight pre-commit: run staged linters/formatters only
echo "� Running lint-staged..."

npm run lint:staged || {
  echo "❌ Pre-commit failed (lint-staged)"
  exit 1
}

echo "✅ Pre-commit passed"
